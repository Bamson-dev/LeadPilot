# Local testing — build from monorepo root (same as Coolify).
# Production VPS: docker compose --env-file .env.production up -d --build

version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: leadthur-backend
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      PORT: 3000
      SUPABASE_URL: ${SUPABASE_URL}
      SUPABASE_SERVICE_KEY: ${SUPABASE_SERVICE_KEY}
      SCRAPER_CONCURRENCY: ${SCRAPER_CONCURRENCY:-5}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:3000}
      PAYSTACK_SECRET_KEY: ${PAYSTACK_SECRET_KEY:-}
      BREVO_API_KEY: ${BREVO_API_KEY:-}
      BREVO_SENDER_EMAIL: ${BREVO_SENDER_EMAIL:-}
      ADMIN_SECRET: ${ADMIN_SECRET:-}
    restart: unless-stopped
    shm_size: "512mb"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "--max-time", "3", "http://127.0.0.1:3000/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 90s
