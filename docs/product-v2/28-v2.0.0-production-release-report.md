# LeadThur v2.0.0 — Official Production Release Report

**Date:** 2026-08-07  
**Updated after:** Manual Coolify production redeploy  
**Tag:** `v2.0.0`  
**Release / merge commit:** `bc10b0ba73e56466607e0660141769d562324e80`

---

## Decision

# **GO — Production deploy verified**

Backend is live on the tagged release. Core deploy gates, observability route shape, CORS, and public FE surfaces pass.

**Residual (operator):** One live authenticated purchase → activation → dashboard → outreach journey was **not** executed from this session (requires production license + payment). Coolify env UI was **not** opened here; forbidden-flag absence is **inferred** from full API availability (not health-only mode).

---

## Final SHAs

| Item | SHA / value |
|------|-------------|
| Production / merge commit | `bc10b0ba73e56466607e0660141769d562324e80` |
| Tag object | `603748cd298d4d7f26540b722187bdc57e6dfe3d` |
| Tag → commit | `bc10b0ba73e56466607e0660141769d562324e80` |
| `origin/main` | `bc10b0ba73e56466607e0660141769d562324e80` |
| **Live production backend** | **`bc10b0ba73e56466607e0660141769d562324e80`** — **MATCH** |
| Frontend | Live on Vercel; **Built for Your Business** present on `/` and `/about`. Exact Vercel git SHA not machine-readable without Vercel token. |

---

## Steps 1–6 (git promote) — complete

| Step | Status |
|------|--------|
| Working tree classification | Local noise only; nothing deleted |
| RC1 completeness in release tree | **PASS** |
| Merge staging → main | **PASS** (`bc10b0b`) |
| Tag `v2.0.0` + push | **PASS** |
| Tag == `origin/main` | **PASS** |

---

## Step 7 — Deploy

| Service | Status |
|---------|--------|
| Backend (Coolify) | **PASS** — manual redeploy confirmed via `/health` SHA |
| Frontend (Vercel) | **PASS** (content live; SHA unverified by tooling) |
| GHA auto-deploy | Still broken (missing `COOLIFY_DEPLOY_WEBHOOK_URL`) — not required after manual Coolify success |

---

## Step 8 — Deployment verification

| Check | Result |
|-------|--------|
| Backend SHA == `bc10b0b` / `v2.0.0` | **PASS** |
| Frontend serves release marketing section | **PASS** |
| `freeTrialIpCapReady` | **true** |
| Health-only mode (would imply `MOCK_*` / `DEMO_MODE` / `ENABLE_TEST_EMAIL`) | **Not active** — API routes respond (401/202/400), not blanket 404 |
| `NODE_ENV=production` | **Inferred** (Dockerfile-baked); Coolify UI not inspected |
| Coolify env UI audit (no staging creds / no forbidden flags) | **Operator confirm remaining** |

---

## Step 9 — Smoke summary

### Automated / unauthenticated — PASS

| Surface | Result |
|---------|--------|
| Landing, Trial, Checkout, Activate, About, Blog, Privacy, Terms | HTTP **200** |
| Dashboard / Saved / Outreach / Mailboxes / Insights / Affiliate / Billing / Settings / Admin shells | HTTP **200** |
| Built for Your Business + WhatsApp CTA on homepage | **Present** |
| `verify-deployment.sh` | **PASS** |
| Phase 2.1 gate script against production | **PASS** (SHA, FE 200, obs 401, events 202) |

### Authenticated full journey — NOT EXECUTED

Landing → Trial → Search → Paywall → **Paid checkout** → Activation → Dashboard → Discovery → Save → Export → Mailbox → Outreach → Open → Affiliate → Billing → Settings → Logout/Login  

**Blocked without production credentials / real payment.** No automatic fixes attempted.

### API shape checks (no auth)

| Endpoint | Observed | Notes |
|----------|----------|-------|
| `POST /public/events` | **202** | Observability ingest live |
| `/admin/observability/*` | **401** | Protected |
| `/auth/status` | **401** | Protected |
| `/admin/overview` | **401** | Protected |
| `/admin/test-email` | **404** | Not exposed |
| `POST /checkout/initialize` (empty/partial) | **400** | Validation alive |
| `POST /webhooks/paystack` (empty) | **200** | Endpoint reachable |

---

## Step 10 — Infrastructure summary

| Check | Result |
|-------|--------|
| `/health` status | **ok** |
| Browser workers | **ready** |
| Queue | **bullmq** (healthy) |
| Memory | **safe** |
| DeepSeek | **configured** |
| Redis (via queue mode) | **Implied healthy** |
| Observability ingest | **202** on public events |
| Admin observability / executive / alerts / funnels / kpis / cohorts / attribution | **401** unauth (routes exist) |
| Unexpected 500s on probed URLs | **None** |

SMTP send / mailbox connect / live webhook signature validation: **not fully exercised** without authenticated journey.

---

## Step 11 — Security summary

| Check | Result |
|-------|--------|
| Admin routes protected | **PASS** (401) |
| Admin observability protected | **PASS** (401) |
| `/public/events` working | **PASS** (202) |
| Test email route not exposed | **PASS** (404) |
| CORS allows `https://www.leadthur.com` | **PASS** |
| FE HSTS | **PASS** |
| RLS on production Supabase | **UNVERIFIED** (MCP not connected to prod project / timeout) |

---

## Exact prior failure (resolved)

| Item | Detail |
|------|--------|
| Failure | Backend was on `8a2af24` |
| Cause | GHA missing Coolify webhook; auto-deploy skipped |
| Fix | **Manual Coolify redeploy** by operator |
| Current | Backend SHA **`bc10b0b`** |

---

## Final recommendation

**GO** — LeadThur **v2.0.0** is deployed to production backend and public frontend surfaces reflect the release.

### Operator follow-ups (post-GO QA)

1. Confirm Coolify production env UI: no `MOCK_*`, `DEMO_MODE`, `ENABLE_TEST_EMAIL`; `NODE_ENV=production`; no staging URLs/keys.  
2. Confirm Vercel Production deployment commit = `bc10b0b`.  
3. Run one live paid customer journey end-to-end.  
4. Set GitHub secret `COOLIFY_DEPLOY_WEBHOOK_URL` so future `main` pushes auto-deploy.
